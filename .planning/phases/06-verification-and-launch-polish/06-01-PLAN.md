---
phase: 06-verification-and-launch-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - playwright.config.ts
  - tests/e2e/fixtures/launcher.ts
  - src/features/shell/components/AppIconButton.tsx
  - src/features/runtime/AppSurface.tsx
  - src/features/apps/calculator/CalculatorApp.tsx
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
user_setup: []
must_haves:
  truths:
    - The repo has a browser-level test runner configured for the actual product target.
    - Stable selectors exist for launcher icons, app surfaces, Home pill, and calculator keys.
    - Test helpers can open both browser mode and deterministic standalone mode without manual setup.
  artifacts:
    - Playwright is installed and configured.
    - A reusable launcher fixture/helper exists for shell tests.
    - Stable test identifiers are added to the relevant shell/runtime/calculator surfaces.
  key_links:
    - Browser-mode tests and standalone-mode tests share the same launcher helper.
    - The standalone helper uses the repo’s existing `openos-install-context=standalone` override rather than ad hoc setup.
    - Calculator keys expose stable identifiers suitable for browser testing.
---

<objective>
Add Playwright infrastructure and stable test contracts for the launcher.

Purpose: Create the browser-level verification layer Phase 6 depends on before any end-to-end scenarios are written.
Output: Playwright config, launcher fixtures, and stable selector hooks across the shell/runtime/calculator surface.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-verification-and-launch-polish/06-RESEARCH.md
@src/features/shell/AdaptiveShellFoundation.tsx
@src/features/runtime/AppSurface.tsx
@src/features/apps/calculator/CalculatorApp.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Playwright to the repo and configure a WebKit-first project</name>
  <files>package.json, pnpm-lock.yaml, playwright.config.ts</files>
  <action>Install `@playwright/test@1.59.1`, add a dedicated browser-test script, and create a Playwright config with a WebKit iPhone-style project as the primary required target. Keep the config simple and compatible with the current Vite app workflow.</action>
  <verify>`pnpm test:e2e -- --list` or equivalent Playwright dry-run command succeeds.</verify>
  <done>The repo can run Playwright-based browser scenarios in a WebKit-first configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Create reusable browser and standalone launcher helpers</name>
  <files>tests/e2e/fixtures/launcher.ts, playwright.config.ts</files>
  <action>Create a launcher helper that can open the product in browser mode and in deterministic standalone mode using the existing `openos-install-context=standalone` override. Keep it central so the shell-flow and calculator specs do not duplicate setup details.</action>
  <verify>The helper is imported cleanly from a smoke test or config-level usage and Playwright can resolve it.</verify>
  <done>Browser and standalone contexts are standardized for the rest of the phase.</done>
</task>

<task type="auto">
  <name>Task 3: Add stable selector hooks across launcher and Calculator surfaces</name>
  <files>src/features/shell/components/AppIconButton.tsx, src/features/runtime/AppSurface.tsx, src/features/apps/calculator/CalculatorApp.tsx</files>
  <action>Add explicit, stable test selectors for launcher icons, app surfaces, the Home pill, and calculator keys/display. Prefer `data-testid`-style hooks or equivalent explicit selectors over brittle CSS/text-only targeting.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass after the selector hooks are added.</verify>
  <done>The browser scenarios can target launcher and calculator elements through a stable contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
- [ ] Playwright config and browser-test command resolve successfully
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Browser automation can target the shell and Calculator through stable selectors
- Standalone-mode verification is deterministic and reusable
</success_criteria>

<output>
After completion, create `.planning/phases/06-verification-and-launch-polish/06-01-SUMMARY.md`
</output>
