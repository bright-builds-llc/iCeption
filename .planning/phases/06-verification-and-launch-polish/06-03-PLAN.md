---
phase: 06-verification-and-launch-polish
plan: "03"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - tests/e2e/calculator.spec.ts
  - tests/e2e/fixtures/launcher.ts
  - src/features/apps/calculator/CalculatorApp.tsx
  - src/features/apps/calculator/calculator.css
autonomous: true
requirements:
  - QUAL-02
user_setup: []
must_haves:
  truths:
    - Calculator happy-path interactions are browser-verified through the real launcher/runtime path.
    - The Calculator browser scenario uses the same deterministic standalone setup as the shell-flow tests.
    - Any small Calculator polish fixes needed for stable scenario correctness are addressed as part of the test-driven pass.
  artifacts:
    - A dedicated calculator Playwright spec exists.
    - Shared launcher helpers support the calculator path.
    - Any Calculator-specific polish fixes discovered by the browser scenario are committed.
  key_links:
    - The calculator spec launches the app through the shell, not by directly rendering the component.
    - Calculator key interactions are asserted through the stable selector contract.
---

<objective>
Implement browser-level coverage for Calculator’s happy path and final calculator-specific polish.

Purpose: Prove that the first real app is usable end to end through the shell and runtime, not just through unit tests.
Output: Calculator Playwright scenarios plus any small Calculator fixes needed for stable browser-level correctness.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-verification-and-launch-polish/06-RESEARCH.md
@tests/e2e/fixtures/launcher.ts
@src/features/apps/calculator/CalculatorApp.tsx
@src/features/apps/calculator/calculatorState.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Calculator happy-path Playwright scenario</name>
  <files>tests/e2e/calculator.spec.ts, tests/e2e/fixtures/launcher.ts</files>
  <action>Write a browser-level Calculator scenario that launches Calculator through the real shell/runtime path, performs a basic happy path through the keypad, and asserts the display/output along the way.</action>
  <verify>The calculator Playwright scenario passes in the chosen browser project(s).</verify>
  <done>Calculator has browser-level coverage for the main user path.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten Calculator selectors and any scenario-exposed polish</name>
  <files>src/features/apps/calculator/CalculatorApp.tsx, src/features/apps/calculator/calculator.css, tests/e2e/calculator.spec.ts</files>
  <action>Make the minimal Calculator-specific adjustments needed so the browser scenario stays readable, stable, and representative of the intended user experience. Keep this tightly scoped to issues surfaced by the browser path.</action>
  <verify>Calculator scenario passes repeatedly and `pnpm build` remains green.</verify>
  <done>The Calculator browser test is reliable and the covered path feels polished enough to trust.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full browser verification set and lock Phase 6 readiness</name>
  <files>tests/e2e/calculator.spec.ts, tests/e2e/shell-flow.spec.ts</files>
  <action>Run the shell-flow and calculator scenarios together and use the result to lock the final browser verification layer for V1. Fix only if the failing issue is clearly within Phase 6’s verification/polish boundary.</action>
  <verify>Full targeted Playwright suite passes alongside `pnpm test`, `npx tsc --noEmit`, and `pnpm build`.</verify>
  <done>The browser verification layer is ready to back the final milestone state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Targeted calculator Playwright scenario passes
- [ ] Full targeted Playwright verification set passes
- [ ] `pnpm test` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Calculator is browser-verified through the real launcher/runtime path
- The final regression suite is narrow, trustworthy, and phase-appropriate
</success_criteria>

<output>
After completion, create `.planning/phases/06-verification-and-launch-polish/06-03-SUMMARY.md`
</output>
