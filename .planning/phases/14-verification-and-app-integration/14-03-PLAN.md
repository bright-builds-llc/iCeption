---
phase: 14-verification-and-app-integration
plan: "03"
type: execute
wave: 3
depends_on:
  - "14-02"
files_modified:
  - .planning/phases/14-verification-and-app-integration/14-03-SUMMARY.md
  - .planning/phases/14-verification-and-app-integration/14-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - PLAT-03
  - QUAL-03
  - QUAL-04
user_setup: []
must_haves:
  truths:
    - The full milestone verification set passes after the final integration work.
    - The phase closeout leaves audit-ready evidence for the entire `v1.1` milestone.
    - No unresolved integration gap remains between platform metadata, home pages, Settings, Notes, and Browser.
  artifacts:
    - Final Phase 14 summary and verification artifacts exist.
    - Phase-level state files are updated to reflect milestone completion readiness.
    - The final verification run is recorded clearly and narrowly.
  key_links:
    - The final verification set combines the focused app specs and the new integration suite into one trustworthy milestone quality bar.
    - Phase 14 verification ties the shared platform layer to the shipped app surfaces and launcher behavior together.
    - The roadmap/state/requirements updates leave the milestone ready for audit rather than requiring more cleanup phases.
---

<objective>
Run the final milestone verification lock and close the integration phase.

Purpose: Finish the milestone with one final verification pass and leave clean phase-level artifacts that support immediate milestone audit.
Output: Final verification evidence, final Phase 14 summary, and updated planning state showing the milestone is ready for audit.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-verification-and-app-integration/14-RESEARCH.md
@tests/e2e/app-integration.spec.ts
@tests/e2e/settings.spec.ts
@tests/e2e/notes.spec.ts
@tests/e2e/browser-app.spec.ts
@tests/e2e/home-pages.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the final integrated verification set</name>
  <files>tests/e2e/app-integration.spec.ts, tests/e2e/settings.spec.ts, tests/e2e/notes.spec.ts, tests/e2e/browser-app.spec.ts, tests/e2e/home-pages.spec.ts</files>
  <action>Run the integrated launcher suite together with the focused app/browser suites and the repo’s baseline checks. Fix only if a failure is clearly within the final integration/verification scope.</action>
  <verify>`pnpm test`, `pnpm test:e2e --project=webkit-iphone`, `npx tsc --noEmit`, and `pnpm build` all pass.</verify>
  <done>The `v1.1` milestone has one trustworthy final verification baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Write the final integration summary and verification report</name>
  <files>.planning/phases/14-verification-and-app-integration/14-03-SUMMARY.md, .planning/phases/14-verification-and-app-integration/14-VERIFICATION.md</files>
  <action>Record what the phase proved, what suite was run, and how the shared platform layer, page-aware launcher, Settings, Notes, and Browser now fit together as one audited milestone surface.</action>
  <verify>The summary and verification report accurately reflect the final integrated verification set and milestone-readiness outcome.</verify>
  <done>The phase leaves explicit evidence for the milestone audit, not just passing command output.</done>
</task>

<task type="auto">
  <name>Task 3: Update phase-level planning state for milestone audit readiness</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <action>Mark Phase 14 complete, mark its requirements complete, and update state so the next action becomes milestone audit rather than more product work. Keep the changes tightly scoped to phase completion metadata.</action>
  <verify>The phase metadata reflects milestone-audit readiness and remains consistent with the verification report.</verify>
  <done>The project is ready to run the `v1.1` milestone audit immediately after Phase 14.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes
- [ ] `pnpm test:e2e --project=webkit-iphone` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
- [ ] Phase 14 artifacts and state updates reflect milestone-audit readiness
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The final integrated milestone quality bar is locked
- The project is ready for `v1.1` audit immediately after Phase 14
</success_criteria>

<output>
After completion, create `.planning/phases/14-verification-and-app-integration/14-03-SUMMARY.md`
</output>
