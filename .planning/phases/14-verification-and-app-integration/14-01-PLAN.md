---
phase: 14-verification-and-app-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/platform/appDefinitions.test.ts
  - src/features/runtime/appRegistry.test.ts
  - src/features/platform/appDefinitions.ts
  - src/features/runtime/appRegistry.ts
autonomous: true
requirements:
  - PLAT-03
user_setup: []
must_haves:
  truths:
    - Shared platform primitives are demonstrably used by `Settings`, `Notes`, and `Browser`, not just present in the codebase.
    - The platform layer remains the source of truth for implemented-app identity, settings participation, and storage namespace metadata.
    - The phase adds executable evidence for shared platform usage rather than relying only on architectural inference.
  artifacts:
    - Platform-level tests or assertions explicitly cover `Settings`, `Notes`, and `Browser`.
    - The runtime app registry still exposes the shared metadata cleanly after any tightening.
    - Any small metadata adjustments needed to make platform usage explicit are committed.
  key_links:
    - `Settings` app-management metadata is driven from the shared definitions/registry.
    - `Notes` storage namespace is grounded in the shared platform storage convention.
    - `Browser` destination behavior still hangs off the shared app-definition/runtime layer.
---

<objective>
Lock the shared platform usage contract across the milestone apps.

Purpose: Add explicit, executable proof that the shared platform primitives from Phase 10 are actively used by `Settings`, `Notes`, and `Browser`.
Output: Tightened platform/runtime tests and any small metadata corrections needed to make shared platform usage unambiguous.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-verification-and-app-integration/14-RESEARCH.md
@src/features/platform/appDefinitions.ts
@src/features/runtime/appRegistry.ts
@src/features/apps/settings/SettingsApp.tsx
@src/features/apps/notes/NotesApp.tsx
@src/features/apps/browser/BrowserApp.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten platform-definition coverage for implemented milestone apps</name>
  <files>src/features/platform/appDefinitions.test.ts, src/features/platform/appDefinitions.ts</files>
  <action>Add or refine focused tests so the implemented milestone app set, settings participation, and storage namespace expectations are explicitly covered for `Settings`, `Notes`, and `Browser`. Make only small metadata adjustments if a genuine ambiguity appears.</action>
  <verify>`pnpm test` passes with the tightened platform-definition coverage.</verify>
  <done>The shared platform layer has explicit, executable evidence for the milestone apps it is supposed to support.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten runtime-facing selectors around platform usage</name>
  <files>src/features/runtime/appRegistry.ts, src/features/runtime/appRegistry.test.ts</files>
  <action>Refine runtime-facing selectors and tests as needed so `Settings`, `Notes`, and `Browser` are clearly queryable through the shared platform metadata. Keep this narrowly scoped to selectors and integration expectations.</action>
  <verify>`pnpm test`, `npx tsc --noEmit`, and `pnpm build` all pass.</verify>
  <done>The runtime layer cleanly exposes the platform metadata the remaining milestone verification depends on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- PLAT-03 has explicit executable proof across Settings, Notes, and Browser
- Runtime selectors remain stable for the final integration suite
</success_criteria>

<output>
After completion, create `.planning/phases/14-verification-and-app-integration/14-01-SUMMARY.md`
</output>
