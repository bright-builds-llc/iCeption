---
phase: 14-verification-and-app-integration
plan: "02"
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - tests/e2e/app-integration.spec.ts
  - tests/e2e/fixtures/launcher.ts
  - src/features/apps/settings/SettingsApp.tsx
  - src/features/apps/notes/NotesApp.tsx
  - src/features/apps/browser/BrowserApp.tsx
autonomous: true
requirements:
  - QUAL-03
  - QUAL-04
user_setup: []
must_haves:
  truths:
    - Browser-level verification proves multi-page launch/return behavior for `Settings`, `Notes`, and `Browser`.
    - The same integration suite proves the key app-specific milestone behaviors for Notes and Browser.
    - The integration tests use the real launcher/runtime path rather than rendering apps in isolation.
  artifacts:
    - A focused cross-app integration Playwright spec exists.
    - Shared launcher helpers support all three app paths cleanly.
    - Any small app-surface/test-hook adjustments needed for stable integration assertions are committed.
  key_links:
    - App integration tests navigate the paged home shell and launch all three implemented milestone apps.
    - Notes local-only/persistence behavior and Browser fallback behavior are asserted through the shipped UI.
    - Close-to-home restoration is proven on the same page-aware launcher path for each app.
---

<objective>
Add the cross-app integration browser suite for the milestone.

Purpose: Prove the new page/app/platform flows together on the real launcher path, with integrated coverage for `Settings`, `Notes`, and `Browser`.
Output: Cross-app Playwright integration suite plus any minimal testability/stability improvements required for the final milestone proof.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-verification-and-app-integration/14-RESEARCH.md
@tests/e2e/fixtures/launcher.ts
@tests/e2e/home-pages.spec.ts
@tests/e2e/settings.spec.ts
@tests/e2e/notes.spec.ts
@tests/e2e/browser-app.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a focused cross-app launcher integration spec</name>
  <files>tests/e2e/app-integration.spec.ts, tests/e2e/fixtures/launcher.ts</files>
  <action>Create a Playwright integration suite that navigates to the correct home-screen page, launches `Settings`, `Notes`, and `Browser`, and proves each app returns home to the same page it launched from. Keep the assertions behavior-focused.</action>
  <verify>`pnpm test:e2e -- tests/e2e/app-integration.spec.ts --project=webkit-iphone` passes.</verify>
  <done>The new multi-app launcher flow is browser-verified beyond isolated app specs.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the integration suite to cover Notes and Browser milestone-specific behaviors</name>
  <files>tests/e2e/app-integration.spec.ts, src/features/apps/notes/NotesApp.tsx, src/features/apps/browser/BrowserApp.tsx</files>
  <action>Within the integration suite, assert that Notes still shows its local-only warning and Browser still shows its graceful fallback behavior for the designated external destination. Make only the minimal UI/test-hook adjustments needed to keep these integrated assertions stable.</action>
  <verify>The integration suite passes with both app-specific milestone behaviors asserted.</verify>
  <done>The final suite proves not just app opening, but the milestone-defining Notes and Browser behaviors as part of the integrated system.</done>
</task>

<task type="auto">
  <name>Task 3: Keep Settings integration represented in the final launcher suite</name>
  <files>tests/e2e/app-integration.spec.ts, src/features/apps/settings/SettingsApp.tsx</files>
  <action>Ensure the final integration suite includes Settings in a way that proves it still opens through the real launcher, participates in page-return behavior, and exposes its first internal app-management surface without requiring the full dedicated Settings spec to be duplicated.</action>
  <verify>The integration suite keeps all three milestone apps represented while remaining readable and stable.</verify>
  <done>Phase 14 has one coherent launcher-level integration story for the milestone apps.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Targeted app-integration Playwright scenario passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- QUAL-03 and QUAL-04 have integrated launcher-path browser proof
- The milestone apps work together as a coherent system
</success_criteria>

<output>
After completion, create `.planning/phases/14-verification-and-app-integration/14-02-SUMMARY.md`
</output>
