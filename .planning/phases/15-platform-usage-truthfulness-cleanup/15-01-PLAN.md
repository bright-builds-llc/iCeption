---
phase: 15-platform-usage-truthfulness-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/runtime/appRegistry.ts
  - src/features/runtime/appRegistry.test.ts
  - src/features/apps/notes/NotesApp.tsx
  - src/features/platform/appDefinitions.test.ts
autonomous: true
requirements:
  - PLAT-03
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 15-2026-04-08T22-43-32Z
generated_at: 2026-04-08T22:43:32.271Z
must_haves:
  truths:
    - `Notes` resolves its storage namespace through shared runtime/platform metadata instead of reconstructing it from a hardcoded app id.
    - The runtime layer exposes a canonical app-identity selector that app-side code can consume without knowing every launcher-entry variant.
    - The platform truth lives in executable code paths, not only in tests or roadmap prose.
  artifacts:
    - A canonical runtime selector for shared app identity exists.
    - `NotesApp` consumes shared runtime/platform metadata for storage wiring.
    - Focused regression coverage exists for the selector and Notes metadata path.
  key_links:
    - The Notes app-side storage path now hangs off the Phase 10 platform metadata.
    - The canonical selector foundation is reusable by `Browser` and `Settings` in later plans.
    - Duplicate launcher entries can remain intact without forcing app-side hardcoded ids.
---

<objective>
Build the canonical app-side metadata access path and move Notes onto it.

Purpose: Close the clearest `PLAT-03` audit gap first by giving app-side code one narrow runtime/platform selector path, then using it to remove Notes' hardcoded storage namespace wiring.
Output: Canonical runtime selector(s), Notes metadata adoption, and focused regression coverage.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.1-v1.1-MILESTONE-AUDIT.md
@.planning/phases/15-platform-usage-truthfulness-cleanup/15-RESEARCH.md
@src/features/runtime/appRegistry.ts
@src/features/platform/appDefinitions.ts
@src/features/apps/notes/NotesApp.tsx
@src/features/platform/appStorage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical runtime selectors for shared app identity</name>
  <files>src/features/runtime/appRegistry.ts, src/features/runtime/appRegistry.test.ts</files>
  <action>Add one narrow runtime-level selector surface that can resolve canonical app metadata for a shared app experience, even when that experience has multiple launcher entries such as grid and dock Browser variants. Keep the selector semantics explicit and small.</action>
  <verify>`pnpm test` passes with focused runtime-selector coverage.</verify>
  <done>App-side code has a supported way to consume shared platform metadata without hardcoding launcher-entry ids.</done>
</task>

<task type="auto">
  <name>Task 2: Move Notes storage wiring onto shared metadata</name>
  <files>src/features/apps/notes/NotesApp.tsx, src/features/runtime/appRegistry.ts</files>
  <action>Refactor `NotesApp` so it resolves its storage namespace through the shared runtime/platform selector path instead of calling `createAppStorageNamespace("notes")` directly. Keep the Notes app behavior unchanged from the user’s perspective.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass after the Notes refactor.</verify>
  <done>`Notes` now proves app-side consumption of the shared platform storage metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Lock focused regression coverage for the Notes metadata path</name>
  <files>src/features/runtime/appRegistry.test.ts, src/features/platform/appDefinitions.test.ts</files>
  <action>Add or refine focused tests so the canonical selector path and the Notes storage namespace expectation are both explicit and stable. Keep the assertions centered on shared metadata consumption, not generic Notes CRUD behavior.</action>
  <verify>`pnpm test`, `npx tsc --noEmit`, and `pnpm build` all pass.</verify>
  <done>The Notes-side closure of the audit gap is protected against regression before Browser and Settings adopt the same path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `Notes` no longer hardcodes its platform storage identity
- Canonical runtime selectors are ready for Browser and Settings adoption
</success_criteria>

<output>
After completion, create `.planning/phases/15-platform-usage-truthfulness-cleanup/15-01-SUMMARY.md`
</output>
