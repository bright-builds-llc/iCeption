---
phase: 15-platform-usage-truthfulness-cleanup
plan: "02"
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - src/features/runtime/appRegistry.ts
  - src/features/runtime/appRegistry.test.ts
  - src/features/apps/browser/BrowserApp.tsx
  - src/features/apps/settings/SettingsApp.tsx
autonomous: true
requirements:
  - PLAT-03
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 15-2026-04-08T22-43-32Z
generated_at: 2026-04-08T22:43:32.271Z
must_haves:
  truths:
    - `Browser` visibly consumes shared runtime/platform metadata in app-side code rather than being driven only by destination metadata.
    - `Settings` renders one Browser management row backed by canonical shared metadata instead of two launcher-placement rows.
    - The runtime/platform layer remains the source of truth for canonical managed-app identity.
  artifacts:
    - A deduped runtime selector for settings-managed apps exists.
    - `BrowserApp` reads canonical shared app metadata through the runtime layer.
    - `SettingsApp` consumes the deduped managed-app selector.
  key_links:
    - `Browser` adopts the same canonical selector foundation that `Notes` adopted in Plan 01.
    - `Settings` no longer needs ad hoc filtering to avoid duplicate Browser rows.
    - Grid and dock Browser launcher entries remain intact while app-management views collapse to one shared Browser app.
---

<objective>
Move Browser and Settings onto canonical shared app metadata.

Purpose: Finish the app-side platform-metadata adoption story by making Browser consume the shared runtime layer directly and by making Settings use a canonical managed-app view that does not duplicate Browser.
Output: Browser metadata adoption, deduped Settings managed-app surface, and runtime selector coverage for canonical managed apps.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.1-v1.1-MILESTONE-AUDIT.md
@.planning/phases/15-platform-usage-truthfulness-cleanup/15-RESEARCH.md
@src/features/runtime/appRegistry.ts
@src/features/apps/browser/BrowserApp.tsx
@src/features/apps/browser/browserDestinations.ts
@src/features/apps/settings/SettingsApp.tsx
@src/features/platform/appDefinitions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a canonical settings-managed app selector</name>
  <files>src/features/runtime/appRegistry.ts, src/features/runtime/appRegistry.test.ts</files>
  <action>Add a runtime selector that returns canonical managed apps for the Settings surface, collapsing duplicate launcher entries that share one implemented app experience. Keep the dedupe rule grounded in shared runtime/platform metadata rather than a Settings-only special case.</action>
  <verify>`pnpm test` passes with focused selector coverage proving Browser appears once in the canonical managed-app view.</verify>
  <done>The runtime layer can express one managed Browser app while still preserving both launcher placements.</done>
</task>

<task type="auto">
  <name>Task 2: Make Browser consume shared app metadata in app-side code</name>
  <files>src/features/apps/browser/BrowserApp.tsx, src/features/runtime/appRegistry.ts</files>
  <action>Refactor `BrowserApp` so it resolves its canonical Browser definition through the shared runtime layer and uses that metadata meaningfully inside the app surface. Keep the Browser scope narrow and avoid broadening into new browser features.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass after the Browser metadata adoption.</verify>
  <done>`Browser` now visibly participates in the shared platform layer from inside app code rather than only through external tests and destination metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Deduplicate the Settings platform-management surface</name>
  <files>src/features/apps/settings/SettingsApp.tsx, src/features/runtime/appRegistry.ts</files>
  <action>Refactor `SettingsApp` to use the canonical managed-app selector so Browser renders once in the management surface with shared metadata-driven label and storage information. Do not hardcode a Browser exception into the component.</action>
  <verify>`pnpm test`, `npx tsc --noEmit`, and `pnpm build` all pass with the deduped Settings view.</verify>
  <done>The app-management surface reflects canonical shared app identity instead of raw launcher-entry duplication.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `Browser` visibly consumes shared platform metadata in app-side code
- Settings shows one Browser management row driven by canonical shared metadata
</success_criteria>

<output>
After completion, create `.planning/phases/15-platform-usage-truthfulness-cleanup/15-02-SUMMARY.md`
</output>
