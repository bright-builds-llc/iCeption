---
phase: 07-real-installed-boundary-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - tests/e2e/fixtures/launcher.ts
  - src/app/bootstrap/createAppContext.test.ts
autonomous: true
requirements:
  - INST-02
  - QUAL-01
user_setup: []
must_haves:
  truths:
    - Phase 7 no longer depends on the `openos-install-context` query override for installed-boundary browser verification.
    - Browser automation runs against a production-like app server path rather than the Vite dev server.
    - Install-context tests still protect browser, display-mode, legacy standalone, and dev-only override semantics.
  artifacts:
    - A production-like Playwright server configuration exists for boundary verification.
    - Shared launcher helpers can enter browser mode and installed-context mode without the dev-only query override.
    - App-context tests explicitly protect the non-dev installed-boundary path.
  key_links:
    - The browser harness exercises the shipped install-context detection path instead of an app-specific dev shortcut.
    - Installed-context entry is represented through browser signals that flow through `createAppContext()` and `detectInstallContext()`.
    - The updated helper layer remains reusable by the shell-flow and Calculator specs.
---

<objective>
Build a production-like installed-boundary verification harness.

Purpose: Remove the dev-only standalone shortcut from the boundary proof path and make the browser suite exercise the shipped install-context detection semantics.
Output: Production-like Playwright server config, new installed-context helper flow, and install-context tests that guard the new boundary assumptions.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1-v1-MILESTONE-AUDIT.md
@.planning/phases/07-real-installed-boundary-verification/07-RESEARCH.md
@.planning/phases/06-verification-and-launch-polish/06-VERIFICATION.md
@playwright.config.ts
@tests/e2e/fixtures/launcher.ts
@src/app/bootstrap/createAppContext.ts
@src/app/bootstrap/createAppContext.test.ts
@src/lib/platform/detectInstallContext.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the boundary harness onto a production-like server path</name>
  <files>playwright.config.ts</files>
  <action>Replace the Phase 6 `pnpm dev` web-server path with a production-like preview/build-backed server flow that better matches shipped runtime semantics. Keep the configuration simple, deterministic, and compatible with the existing WebKit-first project.</action>
  <verify>`pnpm exec playwright test --list --pass-with-no-tests` succeeds with the updated web-server configuration.</verify>
  <done>The browser suite no longer depends on the Vite dev server for installed-boundary verification.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the dev-only standalone helper with a shipped-signal boundary helper</name>
  <files>tests/e2e/fixtures/launcher.ts, playwright.config.ts</files>
  <action>Create a reusable installed-context entry helper that does not rely on `openos-install-context=standalone`. It should drive the same install-context detection path the shipped app uses, using browser-level signals instead of app-specific query branching.</action>
  <verify>A focused helper-level smoke check or first boundary spec can resolve browser mode and installed-context mode without the query override.</verify>
  <done>The e2e harness can enter browser mode and installed-context mode through a more truthful boundary model.</done>
</task>

<task type="auto">
  <name>Task 3: Tighten app-context coverage around the new boundary path</name>
  <files>src/app/bootstrap/createAppContext.test.ts</files>
  <action>Add or adjust tests so the app bootstrap explicitly protects the non-dev installed-boundary path alongside the existing dev-only override semantics. Keep the test surface focused on install-context interpretation, not UI rendering.</action>
  <verify>`pnpm test` passes and the install-context tests clearly cover non-dev standalone signals.</verify>
  <done>The new boundary harness is backed by unit-level proof for the app bootstrap semantics it depends on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes
- [ ] `pnpm exec playwright test --list --pass-with-no-tests` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The installed-boundary browser harness no longer depends on the dev-only query override
- The updated harness remains reusable by downstream shell and Calculator specs
</success_criteria>

<output>
After completion, create `.planning/phases/07-real-installed-boundary-verification/07-01-SUMMARY.md`
</output>
