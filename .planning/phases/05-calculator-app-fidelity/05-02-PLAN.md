---
phase: 05-calculator-app-fidelity
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/features/apps/calculator/CalculatorApp.tsx
  - src/features/apps/calculator/calculator.css
  - src/features/runtime/runtimeShell.css
  - src/features/motion/motionNavigation.css
autonomous: true
requirements:
  - CALC-01
  - CALC-05
user_setup: []
must_haves:
  truths:
    - Calculator visually reads like the portrait iPhone calculator inside the shared app surface.
    - The display, key hierarchy, and spacing feel intentionally calculator-native rather than generic web form UI.
    - The shared shell/runtime/motion system still wraps Calculator without being visually broken by the deeper app styling.
  artifacts:
    - Dedicated calculator styling exists in its own stylesheet.
    - CalculatorApp layout is no longer the basic placeholder keypad.
    - Runtime/app-surface styling has any necessary small adjustments to host the more faithful Calculator UI.
  key_links:
    - Calculator styling integrates with the existing shared app surface and motion system.
    - Calculator display state maps cleanly onto the visual display region.
---

<objective>
Bring the Calculator UI up to portrait iPhone fidelity inside the existing shell/runtime path.

Purpose: Make the first real app feel authentic, not merely functional.
Output: Portrait calculator layout, visual hierarchy, display treatment, and shell integration polish.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-calculator-app-fidelity/05-RESEARCH.md
@src/features/apps/calculator/CalculatorApp.tsx
@src/features/runtime/AppSurface.tsx
@src/features/motion/MotionLayer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the dedicated calculator stylesheet</name>
  <files>src/features/apps/calculator/calculator.css, src/features/apps/calculator/CalculatorApp.tsx</files>
  <action>Create a dedicated calculator stylesheet for portrait fidelity: display treatment, key sizing, spacing, color hierarchy, and the special wide `0` key. Keep the result strongly aligned with the iPhone portrait calculator feel while still fitting the shared app surface.</action>
  <verify>`pnpm build` passes and the calculator app renders with its own dedicated styles.</verify>
  <done>The calculator no longer looks like a generic shell subpanel.</done>
</task>

<task type="auto">
  <name>Task 2: Polish CalculatorApp layout and hierarchy</name>
  <files>src/features/apps/calculator/CalculatorApp.tsx, src/features/apps/calculator/calculator.css</files>
  <action>Refine the display region, keypad grouping, and button hierarchy so operators, utilities, and number keys are visually distinct in the same way the portrait iPhone calculator is. Preserve the shell/runtime integration; do not break the shared app-surface path.</action>
  <verify>Local calculator render looks calculator-native and `pnpm build` passes.</verify>
  <done>The first real app feels visually authentic within the shell.</done>
</task>

<task type="auto">
  <name>Task 3: Adjust shared app-surface hosting only where fidelity requires it</name>
  <files>src/features/runtime/runtimeShell.css, src/features/motion/motionNavigation.css, src/features/apps/calculator/CalculatorApp.tsx</files>
  <action>Make only the minimal shared-shell adjustments needed so Calculator can fill the app surface convincingly without breaking motion or placeholder app presentation. Avoid over-coupling the shared shell to Calculator-specific assumptions.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass, and Calculator still opens through the shared motion/runtime path.</verify>
  <done>Calculator fidelity improves without turning the shared shell into calculator-specific infrastructure.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
- [ ] Local shell check confirms Calculator opens through the shared runtime and looks portrait-iPhone-like
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Calculator now feels visually authentic inside the existing shell
- Shared runtime/motion behavior remains intact
</success_criteria>

<output>
After completion, create `.planning/phases/05-calculator-app-fidelity/05-02-SUMMARY.md`
</output>
