---
phase: 09-home-screen-pages
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/runtime/appRegistry.ts
  - src/features/runtime/homeScreenRuntime.ts
  - src/features/runtime/homeScreenRuntime.test.ts
  - src/features/shell/data/homeScreenIcons.ts
autonomous: true
requirements:
  - HOME-05
  - HOME-06
  - HOME-07
user_setup: []
must_haves:
  truths:
    - Grid apps are assigned to explicit home-screen pages rather than a single undifferentiated grid.
    - Launcher runtime state tracks an active home page and preserves the launching page while an app is open.
    - The dock remains outside page partitioning.
  artifacts:
    - The runtime app model includes page placement for grid apps.
    - Page-aware selectors/helpers exist for the shell.
    - Unit coverage protects page partitioning and return-home page semantics.
  key_links:
    - Home page placement comes from the shared runtime app model.
    - Open/close runtime state preserves page origin so returning home can restore the correct page.
    - Shell data selectors consume the same page-aware runtime model instead of duplicating placement logic.
---

<objective>
Build the page-aware launcher/runtime foundations.

Purpose: Establish explicit home-page placement and page-return semantics in the shared runtime/app model before any paged shell UI is rendered.
Output: Page-aware app metadata, page-aware launcher runtime state, and focused tests protecting the new foundations.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/phases/09-home-screen-pages/09-RESEARCH.md
@src/features/runtime/appRegistry.ts
@src/features/runtime/homeScreenRuntime.ts
@src/features/shell/data/homeScreenIcons.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the runtime app model with home-page placement</name>
  <files>src/features/runtime/appRegistry.ts, src/features/shell/data/homeScreenIcons.ts</files>
  <action>Add explicit home-page placement for grid apps while keeping dock apps global/non-paged. Expose selectors that the shell can use to retrieve apps by page without duplicating placement logic outside the runtime model.</action>
  <verify>`npx tsc --noEmit` passes and selectors can partition the existing grid inventory into multiple pages.</verify>
  <done>The app model now expresses page placement as real launcher metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Make launcher runtime state page-aware</name>
  <files>src/features/runtime/homeScreenRuntime.ts, src/features/runtime/homeScreenRuntime.test.ts</files>
  <action>Extend the runtime home/open/close model so it tracks an active home page and preserves the launching page through app open/close transitions. Keep the state model explicit and phase-appropriate; do not yet add gesture mechanics here.</action>
  <verify>`pnpm test` passes with focused coverage for active-page changes and return-home page restoration.</verify>
  <done>The runtime can remember and restore the correct home page after app navigation.</done>
</task>

<task type="auto">
  <name>Task 3: Protect page partitioning and dock isolation with tests</name>
  <files>src/features/runtime/homeScreenRuntime.test.ts, src/features/shell/data/homeScreenIcons.ts</files>
  <action>Add tests that prove grid apps are partitioned into pages as intended, dock apps stay global, and launcher navigation preserves the originating page across close-to-home transitions.</action>
  <verify>`pnpm test`, `npx tsc --noEmit`, and `pnpm build` all pass.</verify>
  <done>The page-aware launcher foundations are locked before paged shell UI work begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The launcher/runtime is page-aware before UI pagination begins
- Dock behavior remains global and unaffected by page partitioning
</success_criteria>

<output>
After completion, create `.planning/phases/09-home-screen-pages/09-01-SUMMARY.md`
</output>
