---
phase: 09-home-screen-pages
plan: "03"
type: execute
wave: 3
depends_on:
  - "09-02"
files_modified:
  - tests/e2e/home-pages.spec.ts
  - tests/e2e/fixtures/launcher.ts
  - src/features/shell/components/HomeScreenPages.tsx
  - src/features/shell/AdaptiveShellFoundation.tsx
autonomous: true
requirements:
  - HOME-05
  - HOME-06
  - HOME-07
user_setup: []
must_haves:
  truths:
    - Browser-level verification proves page navigation and return-home page restoration on the real shell path.
    - Existing shell-flow behavior remains intact after pagination is introduced.
    - The paged shell is stable enough to support later Settings, Notes, and Browser phases.
  artifacts:
    - A focused home-pages Playwright spec exists.
    - Shared launcher helpers support active-page navigation and app launch from non-default pages.
    - Any final page-shell stabilization fixes required by the tests are committed.
  key_links:
    - The new browser scenario uses the real shell/runtime/motion path, not component-only rendering.
    - Page navigation and close-to-home restoration are asserted through the same launcher runtime that later apps will use.
    - Existing browser-mode shell coverage remains compatible with the new paged shell.
---

<objective>
Verify the paged home-screen flow and finalize Phase 9 readiness.

Purpose: Add narrow browser-level proof that multi-page navigation and return-home behavior work on the real shell path, then make only the stabilization fixes needed to trust the result.
Output: Focused home-pages browser verification plus any small final shell/runtime polish needed for stable page behavior.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-home-screen-pages/09-RESEARCH.md
@tests/e2e/fixtures/launcher.ts
@tests/e2e/shell-flow.spec.ts
@src/features/shell/AdaptiveShellFoundation.tsx
@src/features/shell/components/HomeScreenPages.tsx
@src/features/runtime/homeScreenRuntime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a focused browser scenario for multi-page home navigation</name>
  <files>tests/e2e/home-pages.spec.ts, tests/e2e/fixtures/launcher.ts</files>
  <action>Write a focused Playwright scenario that proves users can move between home-screen pages on the real shell path and that the active page is reflected by the page indicators or equivalent browser-visible shell state. Keep the assertions behavior-focused.</action>
  <verify>`pnpm test:e2e -- tests/e2e/home-pages.spec.ts --project=webkit-iphone` passes.</verify>
  <done>The new paged home-shell behavior is browser-verified rather than only locally eyeballed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify launch from a non-default page returns home to the same page</name>
  <files>tests/e2e/home-pages.spec.ts, tests/e2e/fixtures/launcher.ts, src/features/shell/AdaptiveShellFoundation.tsx</files>
  <action>Extend the focused browser scenario to launch an app from a non-default page and verify close-to-home restores that same page. Make only the minimal shell/runtime fixes required if the page-origin contract is not yet stable enough.</action>
  <verify>The focused home-pages scenario passes with page-return assertions intact.</verify>
  <done>The page-return requirement is locked through the real launcher/motion path.</done>
</task>

<task type="auto">
  <name>Task 3: Run the final narrow verification set for Phase 9</name>
  <files>tests/e2e/home-pages.spec.ts, tests/e2e/shell-flow.spec.ts</files>
  <action>Run the home-pages scenario together with the existing shell-flow coverage and the repo’s baseline checks. Use the result to finalize Phase 9 without broadening into future app phases.</action>
  <verify>`pnpm test`, `pnpm test:e2e --project=webkit-iphone`, `npx tsc --noEmit`, and `pnpm build` all pass.</verify>
  <done>The multi-page launcher is ready to support the rest of `v1.1`.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Targeted home-pages Playwright scenario passes
- [ ] `pnpm test` passes
- [ ] `pnpm test:e2e --project=webkit-iphone` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Multi-page navigation and return-home semantics are browser-verified
- The paged shell is stable enough for the next app phases
</success_criteria>

<output>
After completion, create `.planning/phases/09-home-screen-pages/09-03-SUMMARY.md`
</output>
