---
phase: 09-home-screen-pages
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - src/features/shell/AdaptiveShellFoundation.tsx
  - src/features/shell/components/HomeScreenGrid.tsx
  - src/features/shell/components/HomeScreenPages.tsx
  - src/features/shell/layout/homeScreenLayout.css
  - src/features/shell/shellFoundation.css
autonomous: true
requirements:
  - HOME-05
  - HOME-06
  - HOME-07
user_setup: []
must_haves:
  truths:
    - Users can navigate between multiple home-screen pages in the visible shell.
    - The shell indicates the active page in a way that feels native to the home-screen context.
    - Returning home from an app restores the page the user launched from.
  artifacts:
    - A paged home-screen shell component or equivalent paged grid structure exists.
    - Page indicators are rendered and driven by the page-aware runtime state.
    - The dock remains fixed while only the grid pages change.
  key_links:
    - The paged shell consumes the page-aware runtime selectors and active-page state from Plan 01.
    - Launching an app from page 2+ records enough context that close-to-home restores that page.
    - Page indicators reflect the same active-page state that drives the visible shell.
---

<objective>
Render the paged home-screen shell and page indicators.

Purpose: Turn the new page-aware runtime model into a believable multi-page home-screen experience without weakening the existing shell illusion.
Output: Paged shell UI, page indicators, active-page navigation behavior, and return-home restoration on the visible shell.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-home-screen-pages/09-RESEARCH.md
@src/features/shell/AdaptiveShellFoundation.tsx
@src/features/shell/components/HomeScreenGrid.tsx
@src/features/shell/layout/homeScreenLayout.css
@src/features/shell/shellFoundation.css
@src/features/runtime/homeScreenRuntime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce a paged home-screen shell surface</name>
  <files>src/features/shell/components/HomeScreenPages.tsx, src/features/shell/components/HomeScreenGrid.tsx, src/features/shell/layout/homeScreenLayout.css</files>
  <action>Create a paged home-screen grid surface that renders one active page at a time from the shared page-aware runtime model. Keep the dock global and fixed, and keep the shell visually close to the existing iPhone-like composition.</action>
  <verify>`pnpm build` passes and the shell renders multiple distinct page surfaces from the runtime data.</verify>
  <done>The home shell can visibly represent more than one page without collapsing into a generic tab UI.</done>
</task>

<task type="auto">
  <name>Task 2: Add active-page navigation and indicators</name>
  <files>src/features/shell/AdaptiveShellFoundation.tsx, src/features/shell/components/HomeScreenPages.tsx, src/features/shell/layout/homeScreenLayout.css</files>
  <action>Wire page navigation into the page-aware runtime state and add iPhone-like page indicators that reflect the active page. Navigation may use phase-appropriate gesture or indicator interaction, but it must feel intentional and not purely decorative.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass, and the active page visibly changes through the intended UI path.</verify>
  <done>Users can navigate pages and understand which page is active.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve launch/return semantics across pages</name>
  <files>src/features/shell/AdaptiveShellFoundation.tsx, src/features/runtime/homeScreenRuntime.ts, src/features/shell/components/HomeScreenPages.tsx</files>
  <action>Ensure app launches from non-default pages and close-to-home behavior restore the correct page on the visible shell. Make only the minimal shell/runtime adjustments needed to keep this contract coherent with the existing motion path.</action>
  <verify>Local shell interaction or targeted verification shows “launch from page 2 → return home to page 2” works reliably, and `pnpm build` remains green.</verify>
  <done>The paged shell feels spatially consistent instead of snapping back to page 1 after app navigation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
- [ ] Local page navigation and return-home behavior are coherent
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The shell visibly supports multiple home-screen pages
- Page indicators and return-home behavior stay driven by shared runtime state
</success_criteria>

<output>
After completion, create `.planning/phases/09-home-screen-pages/09-02-SUMMARY.md`
</output>
