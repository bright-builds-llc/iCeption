---
phase: 03-home-screen-runtime
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/features/runtime/AppSurface.tsx
  - src/features/runtime/ComingSoonApp.tsx
  - src/features/runtime/runtimeShell.css
  - src/features/shell/AdaptiveShellFoundation.tsx
autonomous: true
requirements:
  - HOME-04
  - RUNT-02
user_setup: []
must_haves:
  truths:
    - Unimplemented apps open as a real full-screen app surface, not a modal or card.
    - Placeholder apps share the same shell framing as implemented apps.
    - The placeholder surface feels calm and polished rather than temporary or hacked together.
  artifacts:
    - A shared app-surface wrapper exists.
    - A dedicated `Coming Soon` app surface exists.
    - Placeholder shell styling exists in a dedicated runtime stylesheet.
  key_links:
    - The shell runtime renders placeholder apps through the shared app-surface wrapper.
    - Placeholder content is driven by runtime app metadata such as app label.
---

<objective>
Create the shared app surface and polished placeholder app path.

Purpose: Make every `coming-soon` app feel like a real launched system surface, not a dead tap or an overlay.
Output: Shared full-screen app wrapper, dedicated placeholder app component, and shell integration for opening placeholder apps.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-home-screen-runtime/03-CONTEXT.md
@.planning/phases/03-home-screen-runtime/03-RESEARCH.md
@src/features/shell/AdaptiveShellFoundation.tsx
@src/features/runtime/appRegistry.ts
@src/features/runtime/homeScreenRuntime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared full-screen app wrapper</name>
  <files>src/features/runtime/AppSurface.tsx, src/features/runtime/runtimeShell.css</files>
  <action>Create a shared full-screen runtime shell wrapper for launched apps. It should feel like a true app surface and be neutral enough to host both the eventual Calculator app and placeholder apps. Keep it calm and minimal in this phase; do not add Home-button or motion chrome yet.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass.</verify>
  <done>The runtime has one reusable full-screen app-surface boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Create the polished placeholder app surface</name>
  <files>src/features/runtime/ComingSoonApp.tsx, src/features/runtime/runtimeShell.css</files>
  <action>Implement a dedicated `ComingSoonApp` surface that renders app name, `Coming Soon`, one short utility sentence, and an intentional empty state. Keep the tone calm and product-like rather than marketing-heavy.</action>
  <verify>Local shell check confirms the placeholder path is full-screen and visually intentional; then `pnpm build` passes.</verify>
  <done>Placeholder apps feel like legitimate system surfaces rather than temporary overlays.</done>
</task>

<task type="auto">
  <name>Task 3: Route `coming-soon` apps through the shared app surface</name>
  <files>src/features/shell/AdaptiveShellFoundation.tsx, src/features/runtime/AppSurface.tsx, src/features/runtime/ComingSoonApp.tsx</files>
  <action>Wire the runtime so any `coming-soon` app uses the shared app surface and the placeholder content. Do not introduce Calculator implementation here; this plan is only about the placeholder path.</action>
  <verify>`pnpm build` passes and local interaction can open a placeholder app into the full-screen surface.</verify>
  <done>The placeholder path is wired through the same runtime shape future real apps will use.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
- [ ] Local runtime shows a full-screen placeholder app surface for a `coming-soon` app
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Placeholder apps are full-screen and shell-native
- The shared app surface is ready for the real app path to use too
</success_criteria>

<output>
After completion, create `.planning/phases/03-home-screen-runtime/03-02-SUMMARY.md`
</output>
