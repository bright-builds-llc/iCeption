---
phase: 03-home-screen-runtime
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/features/apps/calculator/CalculatorApp.tsx
  - src/features/shell/components/AppIconButton.tsx
  - src/features/shell/components/HomeScreenGrid.tsx
  - src/features/shell/components/Dock.tsx
  - src/features/runtime/AppSurface.tsx
  - src/features/shell/layout/homeScreenLayout.css
  - src/features/runtime/homeScreenRuntime.ts
autonomous: true
requirements:
  - HOME-03
  - HOME-04
  - RUNT-02
user_setup: []
must_haves:
  truths:
    - Every visible icon is launchable with no dead taps or disabled states.
    - Tapping an icon produces a short pressed response before opening the app surface.
    - Calculator is the only implemented app and launches through the same runtime path as placeholder apps.
  artifacts:
    - A reusable icon-button component exists for shell app taps.
    - A real Calculator placeholder/host component exists for the implemented app path.
    - Shell interaction styling exists for the pressed-state response.
  key_links:
    - Grid and dock icons both use the shared icon-button interaction path.
    - Runtime app-open state can target both Calculator and `coming-soon` apps.
    - Calculator uses the shared app-surface path rather than bypassing the runtime.
---

<objective>
Implement launchable icons, pressed-state feedback, and the first real app path.

Purpose: Make the home screen feel like a real launcher where every icon opens something, while keeping Calculator as the only implemented app.
Output: Shared icon-button interaction component, runtime open behavior, and Calculator routed through the same app-surface path as placeholder apps.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-home-screen-runtime/03-CONTEXT.md
@.planning/phases/03-home-screen-runtime/03-RESEARCH.md
@src/features/runtime/appRegistry.ts
@src/features/runtime/homeScreenRuntime.ts
@src/features/shell/components/HomeScreenGrid.tsx
@src/features/shell/components/Dock.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a shared launchable icon-button component</name>
  <files>src/features/shell/components/AppIconButton.tsx, src/features/shell/components/HomeScreenGrid.tsx, src/features/shell/components/Dock.tsx, src/features/shell/layout/homeScreenLayout.css</files>
  <action>Create one shared icon-button component that both grid and dock use. It should provide a short pressed-state response and never render disabled visible icons. Keep the interaction brief and immediate, with no long-press or future motion logic mixed in.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass.</verify>
  <done>Every visible icon uses one interaction path with a short pressed-state response.</done>
</task>

<task type="auto">
  <name>Task 2: Add the implemented Calculator app path</name>
  <files>src/features/apps/calculator/CalculatorApp.tsx, src/features/runtime/AppSurface.tsx, src/features/runtime/homeScreenRuntime.ts</files>
  <action>Create a real app content surface for Calculator that is clearly different from the placeholder surface, but still intentionally minimal in this phase. Use the shared app-surface wrapper instead of special-casing the Calculator path directly in the shell.</action>
  <verify>Local runtime check confirms Calculator opens into the implemented app surface, then `pnpm build` passes.</verify>
  <done>Calculator is the only implemented app and launches through the runtime like everything else.</done>
</task>

<task type="auto">
  <name>Task 3: Wire all visible icons into runtime-backed app opening</name>
  <files>src/features/runtime/homeScreenRuntime.ts, src/features/shell/components/HomeScreenGrid.tsx, src/features/shell/components/Dock.tsx, src/features/runtime/AppSurface.tsx</files>
  <action>Connect the shell so every visible icon opens either the implemented Calculator surface or the full-screen placeholder path, depending on app availability. Ensure there are no dead taps and no layout-only shell state left disconnected from runtime behavior.</action>
  <verify>Local shell check confirms every visible icon opens something, and `pnpm build` still passes.</verify>
  <done>The home screen now behaves like a real launcher with one implemented app and the rest routed to placeholders.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
- [ ] Local shell check confirms every visible icon is launchable and Calculator is the only implemented app
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Every visible icon launches through the runtime
- Calculator is implemented through the shared app-surface path, not special-cased around it
</success_criteria>

<output>
After completion, create `.planning/phases/03-home-screen-runtime/03-03-SUMMARY.md`
</output>
