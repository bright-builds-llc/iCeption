---
phase: 06-verification-and-launch-polish
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - tests/e2e/shell-flow.spec.ts
  - tests/e2e/fixtures/launcher.ts
  - src/features/shell/AdaptiveShellFoundation.tsx
  - src/features/runtime/AppSurface.tsx
autonomous: true
requirements:
  - QUAL-01
user_setup: []
must_haves:
  truths:
    - Browser mode verifies the onboarding shell instead of silently falling into the runtime.
    - Standalone-mode verification covers home-screen render, implemented app launch, implemented app dismiss, and placeholder app launch.
    - Any shell/runtime polish needed for those flows is fixed as part of making the tests reliable.
  artifacts:
    - A shell-flow Playwright spec exists.
    - Launcher helper utilities support the shell-flow scenarios.
    - Any shell/runtime selector or polish fixes required for stable shell coverage are committed.
  key_links:
    - The shell-flow spec uses the shared launcher helper.
    - Implemented-app launch and dismiss are asserted through the real runtime/motion path.
    - Placeholder launch is asserted through the same launcher/runtime path, not mocked.
---

<objective>
Implement browser-level coverage for the core shell flow.

Purpose: Prove the launcher and navigation shell work end to end in both browser and deterministic standalone contexts.
Output: Shell-flow Playwright scenarios plus any small shell/runtime fixes needed to make them reliable and high-signal.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-verification-and-launch-polish/06-RESEARCH.md
@tests/e2e/fixtures/launcher.ts
@src/features/shell/AdaptiveShellFoundation.tsx
@src/features/runtime/AppSurface.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the browser-mode onboarding shell scenario</name>
  <files>tests/e2e/shell-flow.spec.ts, tests/e2e/fixtures/launcher.ts</files>
  <action>Write a Playwright scenario that verifies the browser-mode onboarding/install-first experience. Ensure it proves the product does not silently skip into the standalone launcher in normal browser mode.</action>
  <verify>The shell-flow spec runs and the onboarding scenario passes in Playwright.</verify>
  <done>Browser-mode onboarding is covered by an end-to-end launcher scenario.</done>
</task>

<task type="auto">
  <name>Task 2: Write the standalone shell scenario for implemented and placeholder apps</name>
  <files>tests/e2e/shell-flow.spec.ts, tests/e2e/fixtures/launcher.ts</files>
  <action>Extend the shell-flow spec to verify standalone-mode home-screen rendering, Calculator launch, Home-pill dismiss back to home, and placeholder app launch through the same real runtime path.</action>
  <verify>The standalone shell-flow scenarios pass in Playwright.</verify>
  <done>The core launcher path is browser-verified for both implemented and placeholder apps.</done>
</task>

<task type="auto">
  <name>Task 3: Fix any shell/runtime rough edges exposed by the flow tests</name>
  <files>src/features/shell/AdaptiveShellFoundation.tsx, src/features/runtime/AppSurface.tsx, tests/e2e/shell-flow.spec.ts</files>
  <action>Make only the minimal shell/runtime fixes needed so the shell-flow tests reflect the intended product behavior and stay reliable. Do not broaden scope; fix only issues that the Phase 6 requirements expose.</action>
  <verify>`pnpm test:e2e` (or equivalent targeted shell-flow command) passes along with `pnpm build`.</verify>
  <done>The shell-flow coverage is stable and the covered launcher path feels clean enough to trust.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Targeted shell-flow Playwright scenarios pass
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The shell flow is browser-verified in both browser and standalone contexts
- Small shell polish issues exposed by the tests are resolved
</success_criteria>

<output>
After completion, create `.planning/phases/06-verification-and-launch-polish/06-02-SUMMARY.md`
</output>
