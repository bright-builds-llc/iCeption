---
phase: 15-platform-usage-truthfulness-cleanup
plan: "03"
type: execute
wave: 3
depends_on:
  - "15-02"
files_modified:
  - tests/e2e/browser-app.spec.ts
  - tests/e2e/settings.spec.ts
  - tests/e2e/app-integration.spec.ts
  - src/features/apps/browser/BrowserApp.tsx
  - src/features/apps/settings/SettingsApp.tsx
autonomous: true
requirements:
  - PLAT-03
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 15-2026-04-08T22-43-32Z
generated_at: 2026-04-08T22:43:32.271Z
must_haves:
  truths:
    - Browser-level verification proves the Browser app can switch from embedded to fallback and back to embedded through the real launcher path.
    - Browser-level verification proves Settings exposes a deduped managed-app surface with one Browser row.
    - The phase leaves `v1.1` ready for milestone re-audit with real app-side evidence behind `PLAT-03`.
  artifacts:
    - Focused Browser and Settings Playwright coverage exists for the gap-closure behavior.
    - Any small stabilization fixes needed for the targeted scenarios are committed.
    - The final verification set passes with the cleanup in place.
  key_links:
    - Browser verification exercises the app after shared metadata adoption, not the pre-cleanup code path.
    - Settings verification proves the canonical managed-app selector is what the UI actually renders.
    - The integrated launcher-path suite still holds after the truthfulness cleanup.
---

<objective>
Verify the truthfulness cleanup end to end and close the audit gap.

Purpose: Add the narrow browser-level evidence needed to prove the cleanup is real, then run the full verification set so `v1.1` can be re-audited confidently.
Output: Focused Settings and Browser browser verification, any minimal stabilization fixes, and a green final verification pass.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.1-v1.1-MILESTONE-AUDIT.md
@.planning/phases/15-platform-usage-truthfulness-cleanup/15-RESEARCH.md
@src/features/apps/browser/BrowserApp.tsx
@src/features/apps/settings/SettingsApp.tsx
@tests/e2e/browser-app.spec.ts
@tests/e2e/settings.spec.ts
@tests/e2e/app-integration.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Browser verification to prove fallback -> embedded switching</name>
  <files>tests/e2e/browser-app.spec.ts, src/features/apps/browser/BrowserApp.tsx</files>
  <action>Extend the focused Browser Playwright scenario so it launches through the real shell path, proves the embedded destination, switches to the fallback destination, then switches back to an embedded destination successfully. Keep the assertions deterministic and behavior-focused.</action>
  <verify>`pnpm test:e2e -- tests/e2e/browser-app.spec.ts --project=webkit-iphone` passes.</verify>
  <done>The Browser app is verified for both directions of destination switching, not only embedded -> fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Add Settings/integration coverage for the canonical Browser management row</name>
  <files>tests/e2e/settings.spec.ts, tests/e2e/app-integration.spec.ts, src/features/apps/settings/SettingsApp.tsx</files>
  <action>Add browser-level assertions that the Settings management surface shows one Browser row backed by shared metadata, and keep the integrated launcher-path suite aligned with the deduped managed-app view. Make only the small stabilization fixes needed if the scenario exposes real issues.</action>
  <verify>`pnpm test:e2e -- tests/e2e/settings.spec.ts --project=webkit-iphone` passes.</verify>
  <done>The deduped Settings management surface is proven through the real launcher/runtime path.</done>
</task>

<task type="auto">
  <name>Task 3: Run the final verification set for the gap-closure phase</name>
  <files>tests/e2e/browser-app.spec.ts, tests/e2e/settings.spec.ts, tests/e2e/app-integration.spec.ts</files>
  <action>Run the focused browser scenarios together with the repo baseline verification set, and make only the minimal stabilization fixes needed if the failures are clearly within this phase’s truthfulness-cleanup scope.</action>
  <verify>`pnpm test`, `pnpm test:e2e --project=webkit-iphone`, `npx tsc --noEmit`, and `pnpm build` all pass.</verify>
  <done>The cleanup is ready for milestone re-audit with both app-side and browser-level evidence behind `PLAT-03`.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test:e2e -- tests/e2e/browser-app.spec.ts --project=webkit-iphone` passes
- [ ] `pnpm test:e2e -- tests/e2e/settings.spec.ts --project=webkit-iphone` passes
- [ ] `pnpm test` passes
- [ ] `pnpm test:e2e --project=webkit-iphone` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Browser switching is verified in both directions through the real launcher path
- Settings and integration coverage prove the canonical Browser management view
- `v1.1` is ready for re-audit on `PLAT-03`
</success_criteria>

<output>
After completion, create `.planning/phases/15-platform-usage-truthfulness-cleanup/15-03-SUMMARY.md`
</output>
