---
phase: 05-calculator-app-fidelity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/apps/calculator/calculatorState.ts
  - src/features/apps/calculator/calculatorState.test.ts
  - src/features/apps/calculator/calculatorButtons.ts
  - src/features/apps/calculator/CalculatorApp.tsx
autonomous: true
requirements:
  - CALC-02
  - CALC-03
  - CALC-04
user_setup: []
must_haves:
  truths:
    - Calculator behavior is driven by a pure state/action model rather than ad hoc button-handler logic.
    - Digit, decimal, operator, clear/all clear, sign toggle, percent, and equals behavior are covered by unit tests.
    - The UI renders from stable calculator button metadata rather than repeated hard-coded button markup.
  artifacts:
    - A calculator state module exists with unit coverage.
    - Button metadata exists separately from Calculator JSX.
    - `CalculatorApp` consumes the calculator state/model instead of static placeholder values.
  key_links:
    - Calculator button presses dispatch into the calculator state logic.
    - Display output is rendered from calculator state, not a constant string.
    - Unit tests prove behavior for chained operations and clear-state transitions.
---

<objective>
Build the pure Calculator behavior core and connect it to the app UI.

Purpose: Make Calculator correct before polishing the shell-level visual fidelity.
Output: Action-driven calculator state, behavior tests, keypad metadata, and CalculatorApp wired to real state.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-calculator-app-fidelity/05-RESEARCH.md
@src/features/apps/calculator/CalculatorApp.tsx
@src/features/runtime/AppSurface.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the pure calculator state model</name>
  <files>src/features/apps/calculator/calculatorState.ts, src/features/apps/calculator/calculatorState.test.ts</files>
  <action>Implement a pure calculator reducer/state module that handles digit entry, decimal entry, operator selection, chaining, equals, clear/all clear, sign toggle, and percent. Model the current entry and pending operation explicitly so `%`, `+/-`, and AC/C behavior stay correct. Add high-signal unit coverage for the core sequences and edge cases.</action>
  <verify>`pnpm test -- --run src/features/apps/calculator/calculatorState.test.ts` passes.</verify>
  <done>The calculator logic is explicit, correct for the phase scope, and unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Create button metadata for the portrait keypad</name>
  <files>src/features/apps/calculator/calculatorButtons.ts, src/features/apps/calculator/CalculatorApp.tsx</files>
  <action>Create dedicated keypad metadata that defines button labels, semantic roles, and row placement. Remove the current inline placeholder keypad array from the component so the UI and tests can share a stable source of truth.</action>
  <verify>`npx tsc --noEmit` passes.</verify>
  <done>The portrait keypad is data-driven instead of hard-coded in JSX.</done>
</task>

<task type="auto">
  <name>Task 3: Wire CalculatorApp to the real state model</name>
  <files>src/features/apps/calculator/CalculatorApp.tsx, src/features/apps/calculator/calculatorState.ts</files>
  <action>Refactor `CalculatorApp` so the display and key interactions are driven by the calculator state module and button metadata. Keep the UI intentionally simple for now; this plan is about correctness and state-driven rendering rather than final visual polish.</action>
  <verify>`pnpm test -- --run src/features/apps/calculator/calculatorState.test.ts` and `pnpm build` both pass.</verify>
  <done>CalculatorApp is now a real functional app path instead of a static keypad mock.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test -- --run src/features/apps/calculator/calculatorState.test.ts` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Calculator behavior is correct for the standard portrait interaction model
- CalculatorApp renders from real state and metadata rather than placeholder values
</success_criteria>

<output>
After completion, create `.planning/phases/05-calculator-app-fidelity/05-01-SUMMARY.md`
</output>
