---
phase: 05-calculator-app-fidelity
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - src/features/apps/calculator/calculatorState.test.ts
  - src/features/apps/calculator/CalculatorApp.tsx
  - src/features/apps/calculator/calculator.css
  - src/features/runtime/AppSurface.tsx
  - src/features/runtime/runtimeShell.css
autonomous: true
requirements:
  - CALC-01
  - CALC-02
  - CALC-03
  - CALC-04
  - CALC-05
user_setup: []
must_haves:
  truths:
    - Calculator is the only implemented app and remains routed through the shared app surface and motion path.
    - The calculator behavior and UI together feel phase-complete for the standard portrait use case.
    - The app remains stable after realistic user interaction sequences, not just isolated button presses.
  artifacts:
    - Calculator tests cover the final key interaction sequences and any fixes discovered during UI integration.
    - Calculator app files and shell host files remain internally consistent after fidelity polish.
  key_links:
    - The final calculator UI still reflects the calculator state model accurately.
    - The calculator path still uses the same app-surface and motion shell as every other app.
---

<objective>
Finalize Calculator fidelity and lock it into the shared launcher/runtime path.

Purpose: Ensure the first real app is behaviorally correct, visually convincing, and still integrated with the system shell the project is building toward.
Output: Final calculator polish, updated tests for integrated behavior, and a stable first-app experience inside openOS.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-calculator-app-fidelity/05-RESEARCH.md
@src/features/apps/calculator/CalculatorApp.tsx
@src/features/apps/calculator/calculatorState.ts
@src/features/runtime/AppSurface.tsx
@src/features/motion/MotionLayer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten integrated Calculator behavior coverage</name>
  <files>src/features/apps/calculator/calculatorState.test.ts</files>
  <action>Add any final test sequences needed after UI integration: chained operations, decimal handling, AC/C behavior, sign toggle, percent, and equals sequences that reflect realistic portrait calculator use. Use this task to close any behavior gaps discovered during the UI pass.</action>
  <verify>`pnpm test -- --run src/features/apps/calculator/calculatorState.test.ts` passes.</verify>
  <done>The calculator’s integrated behavior is protected by focused unit coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize Calculator inside the shared shell host</name>
  <files>src/features/apps/calculator/CalculatorApp.tsx, src/features/apps/calculator/calculator.css, src/features/runtime/AppSurface.tsx, src/features/runtime/runtimeShell.css</files>
  <action>Make any final fidelity or host-layout adjustments needed so Calculator feels complete inside the shared shell, without introducing a Calculator-only shell branch. Preserve the existing launcher and motion integration.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass.</verify>
  <done>The first real app feels stable and deliberate inside the product shell.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Calculator remains the canonical implemented app path</name>
  <files>src/features/apps/calculator/CalculatorApp.tsx, src/features/runtime/AppSurface.tsx</files>
  <action>Do a final pass to ensure Calculator remains the sole implemented app path, that it still opens through the shared runtime/motion path, and that no placeholder-specific assumptions leaked into the Calculator experience. Adjust only if required to keep that contract true.</action>
  <verify>`pnpm test`, `npx tsc --noEmit`, and `pnpm build` all pass together.</verify>
  <done>Calculator is phase-complete and remains fully integrated with the shared shell/runtime path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test -- --run src/features/apps/calculator/calculatorState.test.ts` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
- [ ] Local shell check confirms Calculator remains the only implemented app path and feels phase-complete
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Calculator feels like the first real app, not a demo keypad
- Shared shell/runtime/motion integration remains intact
</success_criteria>

<output>
After completion, create `.planning/phases/05-calculator-app-fidelity/05-03-SUMMARY.md`
</output>
