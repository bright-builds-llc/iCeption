---
phase: 07-real-installed-boundary-verification
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - tests/e2e/fixtures/launcher.ts
  - tests/e2e/shell-flow.spec.ts
  - tests/e2e/calculator.spec.ts
  - tests/e2e/installed-boundary.spec.ts
autonomous: true
requirements:
  - INST-02
  - QUAL-01
  - QUAL-02
user_setup: []
must_haves:
  truths:
    - Browser-mode verification and installed-context verification are asserted as distinct entry paths.
    - The installed-entry proof path no longer reports `dev-override` as its install source.
    - Existing shell-flow and Calculator coverage still pass after the boundary harness changes.
  artifacts:
    - A dedicated installed-boundary Playwright spec exists.
    - Shell-flow and Calculator specs use the new installed-context helper path.
    - Any small stabilization changes required by production-like runtime timing are committed.
  key_links:
    - Installed-boundary verification proves browser mode stays in onboarding while installed-context mode reaches the standalone shell branch.
    - Downstream launcher and Calculator scenarios reuse the same boundary helper instead of branching back to the query override.
    - Installed-context assertions are made against shipped app-state markers such as install context/source, not only helper assumptions.
---

<objective>
Prove the installed boundary in browser automation and keep downstream coverage intact.

Purpose: Add explicit end-to-end proof that browser entry and installed entry diverge correctly, then preserve the existing shell and Calculator regression suite on the new boundary harness.
Output: Boundary-focused Playwright coverage plus migrated shell/Calculator scenarios on top of the new installed-context path.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1-v1-MILESTONE-AUDIT.md
@.planning/phases/07-real-installed-boundary-verification/07-RESEARCH.md
@playwright.config.ts
@tests/e2e/fixtures/launcher.ts
@tests/e2e/shell-flow.spec.ts
@tests/e2e/calculator.spec.ts
@src/app/AppRoot.tsx
@src/features/install/browser/BrowserInstallFlow.tsx
@src/features/install/standalone/StandaloneEntry.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an explicit installed-boundary browser scenario</name>
  <files>tests/e2e/installed-boundary.spec.ts, tests/e2e/fixtures/launcher.ts</files>
  <action>Write a focused boundary spec that proves normal browser entry stays in onboarding while installed-context entry reaches the standalone branch through the new Phase 7 harness. Assert the install source truthfully so the test cannot silently regress back to `dev-override` semantics.</action>
  <verify>The installed-boundary spec passes in the WebKit iPhone project.</verify>
  <done>The milestone blocker has a direct automated proof path instead of only implied downstream coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate shell-flow coverage onto the new installed-context helper</name>
  <files>tests/e2e/shell-flow.spec.ts, tests/e2e/fixtures/launcher.ts</files>
  <action>Update the shell-flow suite so its standalone-path scenarios use the new installed-context helper. Preserve the existing browser-onboarding, implemented-app launch/dismiss, and placeholder-app coverage while making the entry assumptions truthful.</action>
  <verify>`pnpm test:e2e -- tests/e2e/shell-flow.spec.ts --project=webkit-iphone` passes.</verify>
  <done>The shell-flow suite continues to protect the launcher on top of the installed-boundary proof path.</done>
</task>

<task type="auto">
  <name>Task 3: Keep Calculator regression coverage on the same installed-entry path</name>
  <files>tests/e2e/calculator.spec.ts, tests/e2e/fixtures/launcher.ts</files>
  <action>Update the Calculator Playwright scenario to use the same installed-context helper as the boundary and shell-flow specs. Make only narrowly-scoped stabilization changes if the production-like runtime exposes timing or first-launch intro differences.</action>
  <verify>`pnpm test:e2e -- tests/e2e/calculator.spec.ts --project=webkit-iphone` passes.</verify>
  <done>Calculator remains browser-verified through the same truthful installed-entry path as the rest of the shell suite.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Installed-boundary Playwright scenario passes
- [ ] Targeted shell-flow Playwright scenarios pass
- [ ] Targeted Calculator Playwright scenario passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Browser mode and installed-context mode are proven as distinct entry paths
- Shell and Calculator coverage remain green on the new boundary harness
</success_criteria>

<output>
After completion, create `.planning/phases/07-real-installed-boundary-verification/07-02-SUMMARY.md`
</output>
