---
phase: 01-installable-web-app-entry
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/features/install/browser/BrowserInstallFlow.tsx
  - src/features/install/browser/BrowserInstallOverlay.tsx
  - src/features/install/browser/PreviewShell.tsx
  - src/features/install/browser/AppTapIntercept.tsx
  - src/features/install/browser/installPromptState.ts
  - src/features/install/browser/installPromptState.test.ts
  - src/features/install/browser/installPromptStorage.ts
  - src/features/install/browser/installSteps.ts
  - src/features/install/browser/browserInstall.css
autonomous: true
requirements:
  - INST-01
user_setup: []
must_haves:
  truths:
    - Non-standalone Safari users see a full-screen install-first experience by default.
    - Users can glimpse a limited preview of the product without getting full browser-mode use.
    - Dismissed onboarding temporarily downgrades to a smaller persistent install prompt and can re-escalate on deeper interaction.
  artifacts:
    - Browser-mode onboarding components exist with explicit Safari-specific install steps.
    - Prompt-state and dismissal-TTL logic exist with unit coverage.
    - A preview shell and app-tap intercept exist for pre-install browsing.
  key_links:
    - BrowserInstallFlow consumes persisted prompt state instead of hard-coded one-time behavior.
    - Preview interactions route through the install intercept rather than opening real app content.
---

<objective>
Implement the browser-mode install funnel for Safari visitors.

Purpose: Make Phase 1 fulfill the install-guidance requirement without accidentally turning browser mode into the real product.
Output: Full-screen onboarding takeover, limited preview shell, persistent install prompt after dismissal, and re-escalation behavior on deeper interaction.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-installable-web-app-entry/01-CONTEXT.md
@.planning/phases/01-installable-web-app-entry/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the install-first browser onboarding surface</name>
  <files>src/features/install/browser/BrowserInstallFlow.tsx, src/features/install/browser/BrowserInstallOverlay.tsx, src/features/install/browser/installSteps.ts, src/features/install/browser/browserInstall.css</files>
  <action>Implement a full-screen onboarding takeover for Safari browser mode that clearly shows the literal install path and lets the product peek through beneath it. Keep the messaging specific to the approved install flow and avoid generic “install this app” copy that assumes other browser capabilities.</action>
  <verify>Run the app locally and confirm browser-mode renders the full takeover instead of the standalone entry branch; then run `pnpm build`.</verify>
  <done>Browser mode shows a focused install-first experience with Safari-specific guidance and restrained preview treatment.</done>
</task>

<task type="auto">
  <name>Task 2: Add limited preview and app-tap interception</name>
  <files>src/features/install/browser/PreviewShell.tsx, src/features/install/browser/AppTapIntercept.tsx, src/features/install/browser/BrowserInstallFlow.tsx</files>
  <action>Implement a shallow preview shell that hints at the future home-screen experience without granting meaningful app use. Route icon/app taps to a polished install intercept rather than opening real functionality, and keep the preview deliberately limited so Phase 1 does not absorb Phase 2 work.</action>
  <verify>Manual local check confirms preview interactions stay gated, then `pnpm build` passes.</verify>
  <done>Users can glimpse the product, but deeper interaction still reinforces the install path.</done>
</task>

<task type="auto">
  <name>Task 3: Persist dismissal state with a temporary downgrade path</name>
  <files>src/features/install/browser/installPromptState.ts, src/features/install/browser/installPromptState.test.ts, src/features/install/browser/installPromptStorage.ts, src/features/install/browser/BrowserInstallFlow.tsx</files>
  <action>Keep onboarding dismissal logic as a pure, unit-tested state helper backed by a tiny storage adapter. Implement the approved behavior: full-screen takeover initially, smaller persistent install prompt after dismissal, then stronger prompt return when the user tries to go deeper later.</action>
  <verify>`pnpm test -- --run installPromptState` passes and `pnpm build` still passes.</verify>
  <done>Dismissal memory is temporary, testable, and consistent with the approved browser-mode funnel.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test -- --run` passes for prompt-state logic
- [ ] `pnpm build` passes
- [ ] Local browser-mode behavior matches the approved funnel
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Browser mode clearly funnels users toward install
- Preview mode remains intentionally limited
</success_criteria>

<output>
After completion, create `.planning/phases/01-installable-web-app-entry/01-02-SUMMARY.md`
</output>
