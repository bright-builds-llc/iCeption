---
phase: 02-adaptive-shell-foundation
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/features/shell/components/AmbientBackground.tsx
  - src/features/shell/theme/ambientPalette.ts
  - src/features/shell/theme/resolveAmbientMotion.ts
  - src/features/shell/theme/resolveAmbientMotion.test.ts
  - src/features/shell/theme/shellTheme.css
autonomous: true
requirements:
  - HOME-01
user_setup: []
must_haves:
  truths:
    - The standalone shell renders one default animated ambient background behind the home-screen chrome.
    - The background motion is slow, fluid, and subtle rather than obviously looping or overpowering the shell.
    - Reduced-motion preferences are respected by toning down or disabling non-essential wallpaper movement.
  artifacts:
    - Ambient palette configuration exists in a dedicated theme module.
    - Ambient motion resolution exists with unit coverage.
    - The shell theme defines restrained material tokens for the shell surface and dock.
  key_links:
    - `AdaptiveShellFoundation` renders the `AmbientBackground` layer behind shell chrome.
    - Ambient motion behavior is derived from a pure helper rather than scattered inline conditions.
    - Dock and shell surfaces consume the same theme token layer that the wallpaper uses.
---

<objective>
Implement the ambient wallpaper and shell material system for the adaptive home-screen foundation.

Purpose: Give the shell its distinct visual tone and the single default animated background that differentiates it from stock iOS without overwhelming the layout.
Output: A default ambient background layer, theme tokens, and reduced-motion-aware wallpaper behavior.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-adaptive-shell-foundation/02-CONTEXT.md
@.planning/phases/02-adaptive-shell-foundation/02-RESEARCH.md
@src/features/shell/AdaptiveShellFoundation.tsx
@src/features/shell/profile/createShellProfile.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the initial ambient palette and shell material tokens</name>
  <files>src/features/shell/theme/ambientPalette.ts, src/features/shell/theme/shellTheme.css</files>
  <action>Define one default palette for the Phase 2 ambient background, informed by the approved cool/cinematic direction, while keeping the theme code extensible enough for later preset work without surfacing selection UI now. Define shell material tokens so the dock and shell surfaces feel frosted and premium without becoming noisy.</action>
  <verify>`npx tsc --noEmit` passes and the shell theme loads in the standalone scene.</verify>
  <done>The shell has a coherent theme token layer and one default ambient palette.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the animated ambient background layer</name>
  <files>src/features/shell/components/AmbientBackground.tsx, src/features/shell/theme/shellTheme.css</files>
  <action>Build the background as layered gradient or blurred-shape planes that drift slowly and subtly behind the shell chrome. Keep the effect liquid or nebula-like, avoid obvious loops, and ensure the background never competes with icon/label legibility.</action>
  <verify>Local standalone shell shows continuous but restrained background motion, then `pnpm build` passes.</verify>
  <done>The shell has one animated ambient wallpaper that clearly differs from stock iOS without overwhelming the scene.</done>
</task>

<task type="auto">
  <name>Task 3: Add reduced-motion-aware ambient behavior</name>
  <files>src/features/shell/theme/resolveAmbientMotion.ts, src/features/shell/theme/resolveAmbientMotion.test.ts, src/features/shell/components/AmbientBackground.tsx, src/features/shell/theme/shellTheme.css</files>
  <action>Implement a pure helper that resolves whether full ambient motion, reduced motion, or no motion should be used, and wire the background component to that helper. Respect `prefers-reduced-motion` from the start instead of treating it as a later patch.</action>
  <verify>`pnpm test -- --run src/features/shell/theme/resolveAmbientMotion.test.ts` passes and `pnpm build` still passes.</verify>
  <done>Ambient motion is centrally controlled and reduced-motion-friendly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test -- --run src/features/shell/theme/resolveAmbientMotion.test.ts` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The shell gains a distinct animated ambient background without sacrificing readability
- Reduced-motion behavior exists from the first version of the wallpaper
</success_criteria>

<output>
After completion, create `.planning/phases/02-adaptive-shell-foundation/02-03-SUMMARY.md`
</output>
