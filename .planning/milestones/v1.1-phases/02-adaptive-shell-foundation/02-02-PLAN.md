---
phase: 02-adaptive-shell-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/features/shell/components/StatusBar.tsx
  - src/features/shell/components/HomeScreenGrid.tsx
  - src/features/shell/components/Dock.tsx
  - src/features/shell/data/homeScreenIcons.ts
  - src/features/shell/layout/homeScreenLayout.css
autonomous: true
requirements:
  - INST-03
  - HOME-01
  - HOME-02
user_setup: []
must_haves:
  truths:
    - The standalone shell renders a coherent static home screen with status bar, icon grid, labels, and dock.
    - Grid rhythm and dock placement adapt across profile kinds without turning into a loose gallery.
    - The custom status row sits within a safe reserved shell region rather than colliding with native status content assumptions.
  artifacts:
    - Home-screen icon data exists in a dedicated data module.
    - Status bar, home grid, and dock components are substantive rather than placeholders.
    - Layout CSS uses shell tokens instead of one-off hard-coded measurements.
  key_links:
    - `AdaptiveShellFoundation` passes profile-derived layout data to status bar, grid, and dock presentation.
    - Icon labels are rendered from the home-screen icon data source.
    - Dock placement and top-chrome spacing consume the profile token layer.
---

<objective>
Implement the static shell composition for the portrait iPhone home screen foundation.

Purpose: Deliver the responsive status bar, icon grid, labels, and dock that make the shell immediately read as iPhone-like before app launching exists.
Output: A static but convincing home-screen composition driven by profile tokens.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-adaptive-shell-foundation/02-CONTEXT.md
@.planning/phases/02-adaptive-shell-foundation/02-RESEARCH.md
@src/features/shell/AdaptiveShellFoundation.tsx
@src/features/shell/profile/createShellProfile.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the status bar within a reserved safe-area-aware shell region</name>
  <files>src/features/shell/components/StatusBar.tsx, src/features/shell/layout/homeScreenLayout.css</files>
  <action>Implement a high-fidelity but non-literal status row that shows time, signal/Wi-Fi style indicators, and battery. Keep it inside the shell’s reserved top region rather than drawing into the unsafe native bar space. The styling should feel premium and iPhone-like without copying Apple assets.</action>
  <verify>Run the local standalone shell and confirm the status row stays visually below the reserved top region; then run `pnpm build`.</verify>
  <done>The shell has a convincing top chrome treatment that respects the status-bar research constraint.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the classic home-screen grid and label rhythm</name>
  <files>src/features/shell/components/HomeScreenGrid.tsx, src/features/shell/data/homeScreenIcons.ts, src/features/shell/layout/homeScreenLayout.css</files>
  <action>Render a classic fixed-column home-screen grid with centered labels, profile-driven spacing, and a restrained icon rhythm. Keep the icon count and row structure visually stable across profiles rather than letting the layout become a generic responsive gallery.</action>
  <verify>Manual local check across at least compact and expanded profile/dev states, then `pnpm build`.</verify>
  <done>The shell instantly reads as a home screen, with labels always visible and rhythm preserved across profile kinds.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the dock as a distinct grounded material layer</name>
  <files>src/features/shell/components/Dock.tsx, src/features/shell/layout/homeScreenLayout.css</files>
  <action>Build the dock as a separate, safe-area-aware material layer anchored at the bottom of the shell scene. The dock should feel grounded and distinct from the app grid while still obeying the same profile token system.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass, and the dock placement remains profile-aware in the standalone shell.</verify>
  <done>The static shell composition has a distinct dock and a complete home-screen layout foundation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
- [ ] Local standalone shell shows a coherent static home-screen composition across profile variants
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The shell reads immediately as an iPhone-style home screen
- Grid, labels, dock, and status bar all derive from the shared profile token system
</success_criteria>

<output>
After completion, create `.planning/phases/02-adaptive-shell-foundation/02-02-SUMMARY.md`
</output>
